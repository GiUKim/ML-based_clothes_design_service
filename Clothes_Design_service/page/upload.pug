
doctype html
html
    head
        meta(charset='utf-8')
    body
        h1 hello #{username}
        form(action='upload' method='post' enctype="multipart/form-data")
            input(type='file' name='userfile')
            input(type='submit')